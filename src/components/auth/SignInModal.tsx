
// SignInModal removed — Auth page (`/auth`) and `AuthProvider` handle sign in / sign up flows.
// Keep a small stub default export so imports (if any) don't break. This component intentionally renders nothing.
import React from 'react';

const SignInModal: React.FC = () => null;

export default SignInModal;